# App UI component inventory (`src/components/ui/`)

- Components found: **10**

## `src/components/ui/Badge.tsx`

**Props types detected:** BadgeProps

**CSS vars referenced (`--*`):**

```txt
--bg-surface-raised
--danger-100
--danger-500
--danger-900
--ds-bg-surface-raised
--ds-content-secondary
--ds-content-tertiary
--ds-danger-100
--ds-danger-500
--ds-danger-900
--ds-primary-100
--ds-primary-300
--ds-primary-900
--ds-success-100
--ds-success-500
--ds-success-900
--ds-warning-100
--ds-warning-500
--ds-warning-900
--primary-100
--primary-300
--primary-900
--success-100
--success-500
--success-900
--text-secondary
--text-tertiary
--warning-100
--warning-500
--warning-900
```
**`var(--token)` usages detected (names only):**

```txt
--bg-surface-raised
--danger-100
--danger-500
--danger-900
--ds-bg-surface-raised
--ds-content-secondary
--ds-content-tertiary
--ds-danger-100
--ds-danger-500
--ds-danger-900
--ds-primary-100
--ds-primary-300
--ds-primary-900
--ds-success-100
--ds-success-500
--ds-success-900
--ds-warning-100
--ds-warning-500
--ds-warning-900
--primary-100
--primary-300
--primary-900
--success-100
--success-500
--success-900
--text-secondary
--text-tertiary
--warning-100
--warning-500
--warning-900
```
**Tailwind-ish class strings (heuristic):**

```txt
gap-1.5
h-2 w-2 rounded-full
inline-flex items-center
px-3 py-1 text-xs font-medium
rounded-full
rounded-md
```
## `src/components/ui/Button.tsx`

**Props types detected:** ButtonProps

**CSS vars referenced (`--*`):**

```txt
--bg-surface
--bg-surface-raised
--border-subtle
--focus-ring
--primary-500
--text-primary
--text-secondary
```
**`var(--token)` usages detected (names only):**

```txt
--bg-surface
--bg-surface-raised
--border-subtle
--focus-ring
--primary-500
--text-primary
--text-secondary
```
**Tailwind-ish class strings (heuristic):**

```txt
None
```
## `src/components/ui/Card.tsx`

**Props types detected:** CardProps

**CSS vars referenced (`--*`):**

```txt
--bg-surface
--bg-surface-raised
--border-default
--border-strong
--border-subtle
--ds-bg-surface
--ds-bg-surface-raised
--ds-content-primary
--ds-primary-500
--primary-500
--text-primary
```
**`var(--token)` usages detected (names only):**

```txt
--bg-surface
--bg-surface-raised
--border-default
--border-strong
--border-subtle
--ds-bg-surface
--ds-bg-surface-raised
--ds-content-primary
--ds-primary-500
--primary-500
--text-primary
```
**Tailwind-ish class strings (heuristic):**

```txt
rounded-2xl
```
## `src/components/ui/EmptyState.tsx`

**Props types detected:** EmptyStateProps

**CSS vars referenced (`--*`):**

```txt
--bg-surface
--border-default
--text-primary
--text-secondary
```
**`var(--token)` usages detected (names only):**

```txt
--bg-surface
--border-default
--text-primary
--text-secondary
```
**Tailwind-ish class strings (heuristic):**

```txt
mt-2 text-sm text-[var(--text-secondary)]
rounded-2xl border border-dashed border-[var(--border-default)] bg-[var(--bg-surface)] p-6 text-center
text-base font-semibold text-[var(--text-primary)]
```
## `src/components/ui/ErrorState.tsx`

**Props types detected:** ErrorStateProps

**CSS vars referenced (`--*`):**

```txt
--danger-100
--danger-500
--danger-900
```
**`var(--token)` usages detected (names only):**

```txt
--danger-100
--danger-500
--danger-900
```
**Tailwind-ish class strings (heuristic):**

```txt
mt-2 text-sm text-[var(--danger-100)]/80
rounded-2xl border border-[var(--danger-500)] bg-[var(--danger-900)] p-6 text-center
text-base font-semibold text-[var(--danger-100)]
```
## `src/components/ui/ListItem.tsx`

**Props types detected:** ListItemProps

**CSS vars referenced (`--*`):**

```txt
--bg-base
--border-subtle
--danger-100
--danger-500
--primary-100
--primary-500
--success-100
--success-500
--text-primary
--text-secondary
--text-tertiary
--warning-100
--warning-500
```
**`var(--token)` usages detected (names only):**

```txt
--bg-base
--border-subtle
--danger-100
--danger-500
--primary-100
--primary-500
--success-100
--success-500
--text-primary
--text-secondary
--text-tertiary
--warning-100
--warning-500
```
**Tailwind-ish class strings (heuristic):**

```txt
,
        accentClasses[tone],
        className,
      ]
        .filter(Boolean)
        .join(
flex items-start justify-between gap-3 pl-2
font-semibold text-[var(--text-primary)]
min-w-0 flex-1
mt-1 text-sm text-[var(--text-secondary)]
shrink-0
```
## `src/components/ui/LoadingState.tsx`

**Props types detected:** LoadingStateProps

**CSS vars referenced (`--*`):**

```txt
--bg-surface
--border-default
--primary-300
--text-primary
--text-secondary
```
**`var(--token)` usages detected (names only):**

```txt
--bg-surface
--border-default
--primary-300
--text-primary
--text-secondary
```
**Tailwind-ish class strings (heuristic):**

```txt
mt-2 text-sm text-[var(--text-secondary)]
mx-auto mb-4 h-10 w-10 animate-spin rounded-full border-4 border-[var(--border-default)] border-t-[var(--primary-300)]
rounded-2xl border border-dashed border-[var(--border-default)] bg-[var(--bg-surface)] p-6 text-center
text-base font-semibold text-[var(--text-primary)]
```
## `src/components/ui/MetricCard.tsx`

**Props types detected:** MetricCardProps

**CSS vars referenced (`--*`):**

```txt
--bg-surface-raised
--border-default
--border-subtle
--danger-100
--danger-500
--primary-100
--primary-500
--success-100
--success-500
--text-primary
--text-secondary
--text-tertiary
--warning-100
--warning-500
```
**`var(--token)` usages detected (names only):**

```txt
--bg-surface-raised
--border-default
--border-subtle
--danger-100
--danger-500
--primary-100
--primary-500
--success-100
--success-500
--text-primary
--text-secondary
--text-tertiary
--warning-100
--warning-500
```
**Tailwind-ish class strings (heuristic):**

```txt
before:absolute before:inset-x-0 before:top-0 before:h-2
mt-2 text-sm text-[var(--text-tertiary)]
mt-3 text-3xl font-semibold tracking-tight
relative overflow-hidden bg-[var(--bg-surface-raised)]
text-sm font-medium text-[var(--text-secondary)]
```
## `src/components/ui/ProgressBar.tsx`

**Props types detected:** ProgressBarProps

**CSS vars referenced (`--*`):**

```txt
--bg-surface-raised
--border-subtle
--danger-500
--primary-500
--success-500
--text-secondary
--text-tertiary
--warning-500
```
**`var(--token)` usages detected (names only):**

```txt
--bg-surface-raised
--border-subtle
--danger-500
--primary-500
--success-500
--text-secondary
--text-tertiary
--warning-500
```
**Tailwind-ish class strings (heuristic):**

```txt
mb-2 flex items-center justify-between gap-3
mt-2 text-xs text-[var(--text-tertiary)]
text-xs font-medium text-[var(--text-secondary)]
text-xs font-medium text-[var(--text-tertiary)]
```
## `src/components/ui/cn.ts`

**CSS vars referenced (`--*`):**

```txt
None
```
**`var(--token)` usages detected (names only):**

```txt
None
```
**Tailwind-ish class strings (heuristic):**

```txt
None
```
